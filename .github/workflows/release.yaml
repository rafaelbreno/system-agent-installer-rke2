on:
  release:
    types: [published]

name: Release Build and Push
permissions:
    contents: read
    id-token: write

env:
  IMAGE: system-agent-installer-rke2
  REPO: rancher

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
        fail-fast: true
        matrix:
          include:
            - arch: amd64
              os: linux
            - arch: arm64
              os: linux
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Fix the not-a-git-repository issue
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Set environment variables
        run: |
          echo "ARCH=${{ matrix.arch }}" >> "$GITHUB_ENV"
          echo "GIT_TAG=$(echo "${{ github.ref_name }}" | sed -e 's/+/-/g')" >> "$GITHUB_ENV"

      - name: Download installer
        env:
          PRIME_RIBS: ${{ secrets.PRIME_RIBS }}
        run: scripts/download

      - name: Publish Image - Prime
        uses: rancher/ecm-distro-tools/actions/publish-image@master
        if: ${{ !contains(github.ref_name, '-rc') }}
        env:
          ARCH: ${{ env.ARCH }}
          OS: ${{ matrix.os }}
          PRIME_RIBS: ${{ secrets.PRIME_RIBS }}
        with:
          image: ${{ env.IMAGE }}
          tag: ${{ env.GIT_TAG }}-${{ matrix.os }}-${{ matrix.arch }}
          make-target: push-image

          platforms: ${{ matrix.os }}/${{ matrix.arch }}

          push-to-public: true
          public-repo: ${{ secrets.DOCKER_USERNAME }}
          public-username: ${{ secrets.DOCKER_USERNAME }}
          public-password: ${{ secrets.DOCKER_PASSWORD }}

          push-to-prime: false

  build-windows:
    runs-on: ubuntu-latest
    strategy:
        fail-fast: true
        matrix:
          include:
            - nanoserver-version: ltsc2022
              arch: amd64
              os: windows
            - nanoserver-version: 1809
              arch: amd64
              os: windows
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Fix the not-a-git-repository issue
        run: |
          git config --global --add safe.directory "$GITHUB_WORKSPACE"

      - name: Set environment variables
        run: |
          echo "ARCH=${{ matrix.arch }}" >> "$GITHUB_ENV"
          echo "GIT_TAG=$(echo "${{ github.ref_name }}" | sed -e 's/+/-/g')" >> "$GITHUB_ENV"

      - name: Download installer
        env:
          PRIME_RIBS: ${{ env.PRIME_RIBS }}
        run: scripts/download

      - name: Publish Image - Prime
        uses: rancher/ecm-distro-tools/actions/publish-image@master
        if: ${{ !contains(github.ref_name, '-rc') }}
        env:
          ARCH: ${{ matrix.arch }}
          NANOSERVER_VERSION: ${{ matrix.nanoserver-version }}
          PRIME_RIBS: ${{ env.PRIME_RIBS }}
        with:
          image: ${{ env.IMAGE }}
          tag: ${{ env.GIT_TAG }}-${{ matrix.os }}-${{ matrix.nanoserver-version }}-${{ matrix.arch }}
          make-target: push-image-windows
          platforms: ${{ matrix.os }}/${{ matrix.arch }}

          push-to-public: true
          public-repo: ${{ secrets.DOCKER_USERNAME }}
          public-username: ${{ secrets.DOCKER_USERNAME }}
          public-password: ${{ secrets.DOCKER_PASSWORD }}

          push-to-prime: false

  create-docker-manifest:
    runs-on: ubuntu-latest
    needs: [ build-linux, build-windows ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set environment variables
        run: |
          . scripts/version

      - name: Publish Manifest - Prime
        uses: rancher/ecm-distro-tools/actions/publish-image@master
        if: ${{ !contains(github.ref_name, '-rc') }}
        with:
          image: ${{ env.IMAGE }}
          tag: ${{ github.ref_name }}
          make-target: publish-manifest

          push-to-public: true
          public-repo: ${{ secrets.DOCKER_USERNAME }}
          public-username: ${{ secrets.DOCKER_USERNAME }}
          public-password: ${{ secrets.DOCKER_PASSWORD }}

          push-to-prime: false

      - name: Inspect Prime image
        if: ${{ !contains(github.ref_name, '-rc') }}
        run: docker buildx imagetools inspect ${{ secrets.DOCKER_USERNAME }}/${{ env.IMAGE }}:${{ env.VERSION }}
